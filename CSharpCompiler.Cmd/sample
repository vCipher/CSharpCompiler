int length = 5;
int[] arr = new int[length];
            
for (int i = 0; i < length; i++)
{
    if (i == 3)
        break;

    arr[i] = i * i;
}

for (int i = 0; i < length; i++)
{
    writeLine(arr[i]);
}